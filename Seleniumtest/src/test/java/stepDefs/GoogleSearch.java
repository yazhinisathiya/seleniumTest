package stepDefs;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.Assert;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class GoogleSearch {
	/*
	 * WebDriver driver = new ChromeDriver();
	 * 
	 * @Given("I am on the Google search page") public void
	 * i_am_on_the_google_search_page() {
	 * 
	 * driver.get("https://www.google.com"); String actualTitle=driver.getTitle();
	 * Assert.assertEquals(actualTitle, "Google");
	 * 
	 * }
	 * 
	 * @When("I search for {string}") public void i_search_for(String Keyword) {
	 * driver.findElement(By.name("q")).sendKeys(Keyword);
	 * driver.findElement(By.name("q")).sendKeys(Keys.ENTER); }
	 * 
	 * @Then("I should see search results for {string}") public void
	 * i_should_see_search_results_for(String Title) { String
	 * actualTitle=driver.getTitle(); Assert.assertEquals(actualTitle, Title); }
	 */
}
